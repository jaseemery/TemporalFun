# Temporal Worker Configuration
# Copy this file to .env and update with your actual values

# =================================================================
# BASIC CONFIGURATION
# =================================================================

# Temporal server address
TEMPORAL_SERVER=localhost:7233

# Task queue name (production for active, standby for inactive)
TASK_QUEUE=default

# Blue-green deployment configuration
ENVIRONMENT=blue
WORKER_IDENTITY=worker-1

# =================================================================
# DEPLOYMENT OPTIONS
# =================================================================

# Option 1: Existing Temporal Server
# If you have an existing Temporal server, use its address:
# TEMPORAL_SERVER=your-temporal-server:7233

# Option 2: Local Development with Docker
# Use localhost:7233 when running Temporal services in Docker
# and the worker locally

# Option 3: Fully Containerized
# When using Docker Compose, the worker container uses environment
# variables defined in docker-compose.yml instead of this .env file